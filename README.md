# Quick and dirty bash script to install NetExec

Simply run on Kali or any distro you may like.

```bash
chmod +x installnxc.sh
./installnxc.sh
```

You might have to fix your PATH, so I'll leave you up it.
